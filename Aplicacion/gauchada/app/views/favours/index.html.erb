<h1> Listado de Favores </h1>

<table class="table">

	<tr>
		<th> Nombre </th>
		<th> Descripción </th>
    	<th> Acciones </th>
	</tr>

	<% @favours.each do |favour| %>
		<tr>
			<td> <%= favour.titulo %> </td>
			<td> <%= favour.descripcion %> </td>
			<td>
					<% id = favour.id %>
					<%= id %>
				 <%= link_to "Mostrar", '/favours/show/:id', class: 'btn btn-info btn-sm' %>
				 <%= link_to "Enviar Solicitud", requests_solicitar_path, class: 'btn btn-info btn-sm' %>
			</td>
		</tr>
	<% end %>

	<% if @favours.size == 0 %>
		<tr> <td colspan="3"> No hay favores </td> </tr>
	<% end %>

</table>
